/**
 * Backward-compatible aggregation layer.
 *
 * Existing imports can keep using `lib/types` while implementation is split
 * into smaller modules under `lib/types/`.
 */

export * from './types/constants';
export * from './types/presets';
export * from './types/types';
